This is an extension of 'maps (pmf).txt'

Embedded resources:

    - Extension name is 'embrsrcs'
    - Provides a PAF archive of map-specific resources to be mounted (typically at 'map:/')

    <Embedded resource PAF archive (See 'archives (paf).txt')>

Baked light info:

    - Extension name is 'bkdltinf'
    - Provides information about the lights that were used to bake the lightmaps

    ...

Directional lightmaps:

    - Extension name is 'dirltmaps'
    - Provides directional lightmaps for backends that support it

    ...

Cubemaps:

    - Extension name is 'cubemaps'
    - Provides cubemaps for backends that support it

    ...


