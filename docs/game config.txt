name = # game title
icon = # default icon resource path
vertype = # { 32 | 16.16 | 8.8.16 | 8.16.8 | 16.8.8 | 8.8.8.8 } (if empty, 32, 16.16, 8.8.16, or 8.8.8.8 depending on count of '.' in 'version')
version = # current version (defaults to 0.0)
minver = # minimum compatible version (used for multiplayer) (defaults to the current version)
userdir = # if empty, the game dir name is used
